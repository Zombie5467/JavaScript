// --------------------------------------------------------------
// FreeCodeCamp Es
// Iterar Sobre los elementos de un Arreglo con un Ciclo for
// --------------------------------------------------------------

var mySkills = ['JavaScript', 'html', 'css', 'WordPress', 'Bootstrap'];

for (var i = 0; i < mySkills.length; i++) {
  console.log(mySkills[i].toUpperCase());
}
