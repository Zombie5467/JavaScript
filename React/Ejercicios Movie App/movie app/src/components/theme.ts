import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#1976d2" //azul estándar
        },
        secondary: {
            main: "#9c27b0" //purpura estándar
        },
    },
});

export default theme;